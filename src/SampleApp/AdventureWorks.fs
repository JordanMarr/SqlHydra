// This code was generated by SqlHydra.SqlServer.
namespace SampleApp.AdventureWorks

type Column(reader: System.Data.IDataReader, column) =
        member val Name = column with get,set
        member __.IsNull() = reader.GetOrdinal column |> reader.IsDBNull
        member __.Alias(alias) = __.Name <- alias
        override __.ToString() = __.Name

type RequiredColumn<'T, 'Reader when 'Reader :> System.Data.IDataReader>(reader: 'Reader, getter: int -> 'T, column) =
        inherit Column(reader, column)
        member __.Read(?alias) = alias |> Option.defaultValue __.Name |> reader.GetOrdinal |> getter

type OptionalColumn<'T, 'Reader when 'Reader :> System.Data.IDataReader>(reader: 'Reader, getter: int -> 'T, column) =
        inherit Column(reader, column)
        member __.Read(?alias) = 
            match alias |> Option.defaultValue __.Name |> reader.GetOrdinal with
            | o when reader.IsDBNull o -> None
            | o -> Some (getter o)

type RequiredBinaryColumn<'T, 'Reader when 'Reader :> System.Data.IDataReader>(reader: 'Reader, getValue: int -> obj, column) =
        inherit Column(reader, column)
        member __.Read(?alias) = alias |> Option.defaultValue __.Name |> reader.GetOrdinal |> getValue :?> byte[]

type OptionalBinaryColumn<'T, 'Reader when 'Reader :> System.Data.IDataReader>(reader: 'Reader, getValue: int -> obj, column) =
        inherit Column(reader, column)
        member __.Read(?alias) = 
            match alias |> Option.defaultValue __.Name |> reader.GetOrdinal with
            | o when reader.IsDBNull o -> None
            | o -> Some (getValue o :?> byte[])
        
module dbo =
    [<CLIMutable>]
    type ErrorLog =
        { ErrorLogID: int
          ErrorTime: System.DateTime
          UserName: string
          ErrorNumber: int
          ErrorMessage: string
          ErrorSeverity: Option<int>
          ErrorState: Option<int>
          ErrorProcedure: Option<string>
          ErrorLine: Option<int> }

    type ErrorLogReader(reader: Microsoft.Data.SqlClient.SqlDataReader) =
        member val ErrorLogID = RequiredColumn(reader, reader.GetInt32, "ErrorLogID")
        member val ErrorTime = RequiredColumn(reader, reader.GetDateTime, "ErrorTime")
        member val UserName = RequiredColumn(reader, reader.GetString, "UserName")
        member val ErrorNumber = RequiredColumn(reader, reader.GetInt32, "ErrorNumber")
        member val ErrorMessage = RequiredColumn(reader, reader.GetString, "ErrorMessage")
        member val ErrorSeverity = OptionalColumn(reader, reader.GetInt32, "ErrorSeverity")
        member val ErrorState = OptionalColumn(reader, reader.GetInt32, "ErrorState")
        member val ErrorProcedure = OptionalColumn(reader, reader.GetString, "ErrorProcedure")
        member val ErrorLine = OptionalColumn(reader, reader.GetInt32, "ErrorLine")
        member __.Read() =
            { ErrorLogID = __.ErrorLogID.Read()
              ErrorTime = __.ErrorTime.Read()
              UserName = __.UserName.Read()
              ErrorNumber = __.ErrorNumber.Read()
              ErrorMessage = __.ErrorMessage.Read()
              ErrorSeverity = __.ErrorSeverity.Read()
              ErrorState = __.ErrorState.Read()
              ErrorProcedure = __.ErrorProcedure.Read()
              ErrorLine = __.ErrorLine.Read() }

        member __.ReadIf(column: Column) =
            if column.IsNull() then
                None
            else
                Some(__.Read())

    [<CLIMutable>]
    type BuildVersion =
        { SystemInformationID: byte
          ``Database Version``: string
          VersionDate: System.DateTime
          ModifiedDate: System.DateTime }

    type BuildVersionReader(reader: Microsoft.Data.SqlClient.SqlDataReader) =
        member val SystemInformationID = RequiredColumn(reader, reader.GetByte, "SystemInformationID")
        member val ``Database Version`` = RequiredColumn(reader, reader.GetString, "Database Version")
        member val VersionDate = RequiredColumn(reader, reader.GetDateTime, "VersionDate")
        member val ModifiedDate = RequiredColumn(reader, reader.GetDateTime, "ModifiedDate")
        member __.Read() =
            { SystemInformationID = __.SystemInformationID.Read()
              ``Database Version`` = __.``Database Version``.Read()
              VersionDate = __.VersionDate.Read()
              ModifiedDate = __.ModifiedDate.Read() }

        member __.ReadIf(column: Column) =
            if column.IsNull() then
                None
            else
                Some(__.Read())

module SalesLT =
    [<CLIMutable>]
    type Address =
        { City: string
          StateProvince: string
          CountryRegion: string
          PostalCode: string
          rowguid: System.Guid
          ModifiedDate: System.DateTime
          AddressID: int
          AddressLine1: string
          AddressLine2: Option<string> }

    type AddressReader(reader: Microsoft.Data.SqlClient.SqlDataReader) =
        member val City = RequiredColumn(reader, reader.GetString, "City")
        member val StateProvince = RequiredColumn(reader, reader.GetString, "StateProvince")
        member val CountryRegion = RequiredColumn(reader, reader.GetString, "CountryRegion")
        member val PostalCode = RequiredColumn(reader, reader.GetString, "PostalCode")
        member val rowguid = RequiredColumn(reader, reader.GetGuid, "rowguid")
        member val ModifiedDate = RequiredColumn(reader, reader.GetDateTime, "ModifiedDate")
        member val AddressID = RequiredColumn(reader, reader.GetInt32, "AddressID")
        member val AddressLine1 = RequiredColumn(reader, reader.GetString, "AddressLine1")
        member val AddressLine2 = OptionalColumn(reader, reader.GetString, "AddressLine2")
        member __.Read() =
            { City = __.City.Read()
              StateProvince = __.StateProvince.Read()
              CountryRegion = __.CountryRegion.Read()
              PostalCode = __.PostalCode.Read()
              rowguid = __.rowguid.Read()
              ModifiedDate = __.ModifiedDate.Read()
              AddressID = __.AddressID.Read()
              AddressLine1 = __.AddressLine1.Read()
              AddressLine2 = __.AddressLine2.Read() }

        member __.ReadIf(column: Column) =
            if column.IsNull() then
                None
            else
                Some(__.Read())

    [<CLIMutable>]
    type Customer =
        { LastName: string
          PasswordHash: string
          PasswordSalt: string
          rowguid: System.Guid
          ModifiedDate: System.DateTime
          CustomerID: int
          NameStyle: bool
          FirstName: string
          MiddleName: Option<string>
          Title: Option<string>
          Suffix: Option<string>
          CompanyName: Option<string>
          SalesPerson: Option<string>
          EmailAddress: Option<string>
          Phone: Option<string> }

    type CustomerReader(reader: Microsoft.Data.SqlClient.SqlDataReader) =
        member val LastName = RequiredColumn(reader, reader.GetString, "LastName")
        member val PasswordHash = RequiredColumn(reader, reader.GetString, "PasswordHash")
        member val PasswordSalt = RequiredColumn(reader, reader.GetString, "PasswordSalt")
        member val rowguid = RequiredColumn(reader, reader.GetGuid, "rowguid")
        member val ModifiedDate = RequiredColumn(reader, reader.GetDateTime, "ModifiedDate")
        member val CustomerID = RequiredColumn(reader, reader.GetInt32, "CustomerID")
        member val NameStyle = RequiredColumn(reader, reader.GetBoolean, "NameStyle")
        member val FirstName = RequiredColumn(reader, reader.GetString, "FirstName")
        member val MiddleName = OptionalColumn(reader, reader.GetString, "MiddleName")
        member val Title = OptionalColumn(reader, reader.GetString, "Title")
        member val Suffix = OptionalColumn(reader, reader.GetString, "Suffix")
        member val CompanyName = OptionalColumn(reader, reader.GetString, "CompanyName")
        member val SalesPerson = OptionalColumn(reader, reader.GetString, "SalesPerson")
        member val EmailAddress = OptionalColumn(reader, reader.GetString, "EmailAddress")
        member val Phone = OptionalColumn(reader, reader.GetString, "Phone")
        member __.Read() =
            { LastName = __.LastName.Read()
              PasswordHash = __.PasswordHash.Read()
              PasswordSalt = __.PasswordSalt.Read()
              rowguid = __.rowguid.Read()
              ModifiedDate = __.ModifiedDate.Read()
              CustomerID = __.CustomerID.Read()
              NameStyle = __.NameStyle.Read()
              FirstName = __.FirstName.Read()
              MiddleName = __.MiddleName.Read()
              Title = __.Title.Read()
              Suffix = __.Suffix.Read()
              CompanyName = __.CompanyName.Read()
              SalesPerson = __.SalesPerson.Read()
              EmailAddress = __.EmailAddress.Read()
              Phone = __.Phone.Read() }

        member __.ReadIf(column: Column) =
            if column.IsNull() then
                None
            else
                Some(__.Read())

    [<CLIMutable>]
    type CustomerAddress =
        { CustomerID: int
          AddressID: int
          AddressType: string
          rowguid: System.Guid
          ModifiedDate: System.DateTime }

    type CustomerAddressReader(reader: Microsoft.Data.SqlClient.SqlDataReader) =
        member val CustomerID = RequiredColumn(reader, reader.GetInt32, "CustomerID")
        member val AddressID = RequiredColumn(reader, reader.GetInt32, "AddressID")
        member val AddressType = RequiredColumn(reader, reader.GetString, "AddressType")
        member val rowguid = RequiredColumn(reader, reader.GetGuid, "rowguid")
        member val ModifiedDate = RequiredColumn(reader, reader.GetDateTime, "ModifiedDate")
        member __.Read() =
            { CustomerID = __.CustomerID.Read()
              AddressID = __.AddressID.Read()
              AddressType = __.AddressType.Read()
              rowguid = __.rowguid.Read()
              ModifiedDate = __.ModifiedDate.Read() }

        member __.ReadIf(column: Column) =
            if column.IsNull() then
                None
            else
                Some(__.Read())

    [<CLIMutable>]
    type Product =
        { ProductID: int
          Name: string
          ProductNumber: string
          StandardCost: decimal
          ListPrice: decimal
          SellStartDate: System.DateTime
          rowguid: System.Guid
          ModifiedDate: System.DateTime
          SellEndDate: Option<System.DateTime>
          DiscontinuedDate: Option<System.DateTime>
          ThumbNailPhoto: Option<byte []>
          ThumbnailPhotoFileName: Option<string>
          Size: Option<string>
          Weight: Option<decimal>
          ProductCategoryID: Option<int>
          ProductModelID: Option<int>
          Color: Option<string> }

    type ProductReader(reader: Microsoft.Data.SqlClient.SqlDataReader) =
        member val ProductID = RequiredColumn(reader, reader.GetInt32, "ProductID")
        member val Name = RequiredColumn(reader, reader.GetString, "Name")
        member val ProductNumber = RequiredColumn(reader, reader.GetString, "ProductNumber")
        member val StandardCost = RequiredColumn(reader, reader.GetDecimal, "StandardCost")
        member val ListPrice = RequiredColumn(reader, reader.GetDecimal, "ListPrice")
        member val SellStartDate = RequiredColumn(reader, reader.GetDateTime, "SellStartDate")
        member val rowguid = RequiredColumn(reader, reader.GetGuid, "rowguid")
        member val ModifiedDate = RequiredColumn(reader, reader.GetDateTime, "ModifiedDate")
        member val SellEndDate = OptionalColumn(reader, reader.GetDateTime, "SellEndDate")
        member val DiscontinuedDate = OptionalColumn(reader, reader.GetDateTime, "DiscontinuedDate")
        member val ThumbNailPhoto = OptionalBinaryColumn(reader, reader.GetValue, "ThumbNailPhoto")
        member val ThumbnailPhotoFileName = OptionalColumn(reader, reader.GetString, "ThumbnailPhotoFileName")
        member val Size = OptionalColumn(reader, reader.GetString, "Size")
        member val Weight = OptionalColumn(reader, reader.GetDecimal, "Weight")
        member val ProductCategoryID = OptionalColumn(reader, reader.GetInt32, "ProductCategoryID")
        member val ProductModelID = OptionalColumn(reader, reader.GetInt32, "ProductModelID")
        member val Color = OptionalColumn(reader, reader.GetString, "Color")
        member __.Read() =
            { ProductID = __.ProductID.Read()
              Name = __.Name.Read()
              ProductNumber = __.ProductNumber.Read()
              StandardCost = __.StandardCost.Read()
              ListPrice = __.ListPrice.Read()
              SellStartDate = __.SellStartDate.Read()
              rowguid = __.rowguid.Read()
              ModifiedDate = __.ModifiedDate.Read()
              SellEndDate = __.SellEndDate.Read()
              DiscontinuedDate = __.DiscontinuedDate.Read()
              ThumbNailPhoto = __.ThumbNailPhoto.Read()
              ThumbnailPhotoFileName = __.ThumbnailPhotoFileName.Read()
              Size = __.Size.Read()
              Weight = __.Weight.Read()
              ProductCategoryID = __.ProductCategoryID.Read()
              ProductModelID = __.ProductModelID.Read()
              Color = __.Color.Read() }

        member __.ReadIf(column: Column) =
            if column.IsNull() then
                None
            else
                Some(__.Read())

    [<CLIMutable>]
    type ProductCategory =
        { Name: string
          rowguid: System.Guid
          ModifiedDate: System.DateTime
          ProductCategoryID: int
          ParentProductCategoryID: Option<int> }

    type ProductCategoryReader(reader: Microsoft.Data.SqlClient.SqlDataReader) =
        member val Name = RequiredColumn(reader, reader.GetString, "Name")
        member val rowguid = RequiredColumn(reader, reader.GetGuid, "rowguid")
        member val ModifiedDate = RequiredColumn(reader, reader.GetDateTime, "ModifiedDate")
        member val ProductCategoryID = RequiredColumn(reader, reader.GetInt32, "ProductCategoryID")
        member val ParentProductCategoryID = OptionalColumn(reader, reader.GetInt32, "ParentProductCategoryID")
        member __.Read() =
            { Name = __.Name.Read()
              rowguid = __.rowguid.Read()
              ModifiedDate = __.ModifiedDate.Read()
              ProductCategoryID = __.ProductCategoryID.Read()
              ParentProductCategoryID = __.ParentProductCategoryID.Read() }

        member __.ReadIf(column: Column) =
            if column.IsNull() then
                None
            else
                Some(__.Read())

    [<CLIMutable>]
    type ProductDescription =
        { ProductDescriptionID: int
          Description: string
          rowguid: System.Guid
          ModifiedDate: System.DateTime }

    type ProductDescriptionReader(reader: Microsoft.Data.SqlClient.SqlDataReader) =
        member val ProductDescriptionID = RequiredColumn(reader, reader.GetInt32, "ProductDescriptionID")
        member val Description = RequiredColumn(reader, reader.GetString, "Description")
        member val rowguid = RequiredColumn(reader, reader.GetGuid, "rowguid")
        member val ModifiedDate = RequiredColumn(reader, reader.GetDateTime, "ModifiedDate")
        member __.Read() =
            { ProductDescriptionID = __.ProductDescriptionID.Read()
              Description = __.Description.Read()
              rowguid = __.rowguid.Read()
              ModifiedDate = __.ModifiedDate.Read() }

        member __.ReadIf(column: Column) =
            if column.IsNull() then
                None
            else
                Some(__.Read())

    [<CLIMutable>]
    type ProductModel =
        { ProductModelID: int
          Name: string
          rowguid: System.Guid
          ModifiedDate: System.DateTime
          CatalogDescription: Option<System.Xml.Linq.XElement> }

    type ProductModelReader(reader: Microsoft.Data.SqlClient.SqlDataReader) =
        member val ProductModelID = RequiredColumn(reader, reader.GetInt32, "ProductModelID")
        member val Name = RequiredColumn(reader, reader.GetString, "Name")
        member val rowguid = RequiredColumn(reader, reader.GetGuid, "rowguid")
        member val ModifiedDate = RequiredColumn(reader, reader.GetDateTime, "ModifiedDate")

    [<CLIMutable>]
    type ProductModelProductDescription =
        { ProductModelID: int
          ProductDescriptionID: int
          Culture: string
          rowguid: System.Guid
          ModifiedDate: System.DateTime }

    type ProductModelProductDescriptionReader(reader: Microsoft.Data.SqlClient.SqlDataReader) =
        member val ProductModelID = RequiredColumn(reader, reader.GetInt32, "ProductModelID")
        member val ProductDescriptionID = RequiredColumn(reader, reader.GetInt32, "ProductDescriptionID")
        member val Culture = RequiredColumn(reader, reader.GetString, "Culture")
        member val rowguid = RequiredColumn(reader, reader.GetGuid, "rowguid")
        member val ModifiedDate = RequiredColumn(reader, reader.GetDateTime, "ModifiedDate")
        member __.Read() =
            { ProductModelID = __.ProductModelID.Read()
              ProductDescriptionID = __.ProductDescriptionID.Read()
              Culture = __.Culture.Read()
              rowguid = __.rowguid.Read()
              ModifiedDate = __.ModifiedDate.Read() }

        member __.ReadIf(column: Column) =
            if column.IsNull() then
                None
            else
                Some(__.Read())

    [<CLIMutable>]
    type SalesOrderDetail =
        { SalesOrderID: int
          SalesOrderDetailID: int
          OrderQty: int16
          ProductID: int
          UnitPrice: decimal
          UnitPriceDiscount: decimal
          LineTotal: decimal
          rowguid: System.Guid
          ModifiedDate: System.DateTime }

    type SalesOrderDetailReader(reader: Microsoft.Data.SqlClient.SqlDataReader) =
        member val SalesOrderID = RequiredColumn(reader, reader.GetInt32, "SalesOrderID")
        member val SalesOrderDetailID = RequiredColumn(reader, reader.GetInt32, "SalesOrderDetailID")
        member val OrderQty = RequiredColumn(reader, reader.GetInt16, "OrderQty")
        member val ProductID = RequiredColumn(reader, reader.GetInt32, "ProductID")
        member val UnitPrice = RequiredColumn(reader, reader.GetDecimal, "UnitPrice")
        member val UnitPriceDiscount = RequiredColumn(reader, reader.GetDecimal, "UnitPriceDiscount")
        member val LineTotal = RequiredColumn(reader, reader.GetDecimal, "LineTotal")
        member val rowguid = RequiredColumn(reader, reader.GetGuid, "rowguid")
        member val ModifiedDate = RequiredColumn(reader, reader.GetDateTime, "ModifiedDate")
        member __.Read() =
            { SalesOrderID = __.SalesOrderID.Read()
              SalesOrderDetailID = __.SalesOrderDetailID.Read()
              OrderQty = __.OrderQty.Read()
              ProductID = __.ProductID.Read()
              UnitPrice = __.UnitPrice.Read()
              UnitPriceDiscount = __.UnitPriceDiscount.Read()
              LineTotal = __.LineTotal.Read()
              rowguid = __.rowguid.Read()
              ModifiedDate = __.ModifiedDate.Read() }

        member __.ReadIf(column: Column) =
            if column.IsNull() then
                None
            else
                Some(__.Read())

    [<CLIMutable>]
    type SalesOrderHeader =
        { SalesOrderID: int
          RevisionNumber: byte
          OrderDate: System.DateTime
          DueDate: System.DateTime
          Status: byte
          OnlineOrderFlag: bool
          SalesOrderNumber: string
          CustomerID: int
          SubTotal: decimal
          TaxAmt: decimal
          Freight: decimal
          TotalDue: decimal
          rowguid: System.Guid
          ModifiedDate: System.DateTime
          ShipMethod: string
          CreditCardApprovalCode: Option<string>
          Comment: Option<string>
          ShipToAddressID: Option<int>
          BillToAddressID: Option<int>
          PurchaseOrderNumber: Option<string>
          AccountNumber: Option<string>
          ShipDate: Option<System.DateTime> }

    type SalesOrderHeaderReader(reader: Microsoft.Data.SqlClient.SqlDataReader) =
        member val SalesOrderID = RequiredColumn(reader, reader.GetInt32, "SalesOrderID")
        member val RevisionNumber = RequiredColumn(reader, reader.GetByte, "RevisionNumber")
        member val OrderDate = RequiredColumn(reader, reader.GetDateTime, "OrderDate")
        member val DueDate = RequiredColumn(reader, reader.GetDateTime, "DueDate")
        member val Status = RequiredColumn(reader, reader.GetByte, "Status")
        member val OnlineOrderFlag = RequiredColumn(reader, reader.GetBoolean, "OnlineOrderFlag")
        member val SalesOrderNumber = RequiredColumn(reader, reader.GetString, "SalesOrderNumber")
        member val CustomerID = RequiredColumn(reader, reader.GetInt32, "CustomerID")
        member val SubTotal = RequiredColumn(reader, reader.GetDecimal, "SubTotal")
        member val TaxAmt = RequiredColumn(reader, reader.GetDecimal, "TaxAmt")
        member val Freight = RequiredColumn(reader, reader.GetDecimal, "Freight")
        member val TotalDue = RequiredColumn(reader, reader.GetDecimal, "TotalDue")
        member val rowguid = RequiredColumn(reader, reader.GetGuid, "rowguid")
        member val ModifiedDate = RequiredColumn(reader, reader.GetDateTime, "ModifiedDate")
        member val ShipMethod = RequiredColumn(reader, reader.GetString, "ShipMethod")
        member val CreditCardApprovalCode = OptionalColumn(reader, reader.GetString, "CreditCardApprovalCode")
        member val Comment = OptionalColumn(reader, reader.GetString, "Comment")
        member val ShipToAddressID = OptionalColumn(reader, reader.GetInt32, "ShipToAddressID")
        member val BillToAddressID = OptionalColumn(reader, reader.GetInt32, "BillToAddressID")
        member val PurchaseOrderNumber = OptionalColumn(reader, reader.GetString, "PurchaseOrderNumber")
        member val AccountNumber = OptionalColumn(reader, reader.GetString, "AccountNumber")
        member val ShipDate = OptionalColumn(reader, reader.GetDateTime, "ShipDate")
        member __.Read() =
            { SalesOrderID = __.SalesOrderID.Read()
              RevisionNumber = __.RevisionNumber.Read()
              OrderDate = __.OrderDate.Read()
              DueDate = __.DueDate.Read()
              Status = __.Status.Read()
              OnlineOrderFlag = __.OnlineOrderFlag.Read()
              SalesOrderNumber = __.SalesOrderNumber.Read()
              CustomerID = __.CustomerID.Read()
              SubTotal = __.SubTotal.Read()
              TaxAmt = __.TaxAmt.Read()
              Freight = __.Freight.Read()
              TotalDue = __.TotalDue.Read()
              rowguid = __.rowguid.Read()
              ModifiedDate = __.ModifiedDate.Read()
              ShipMethod = __.ShipMethod.Read()
              CreditCardApprovalCode = __.CreditCardApprovalCode.Read()
              Comment = __.Comment.Read()
              ShipToAddressID = __.ShipToAddressID.Read()
              BillToAddressID = __.BillToAddressID.Read()
              PurchaseOrderNumber = __.PurchaseOrderNumber.Read()
              AccountNumber = __.AccountNumber.Read()
              ShipDate = __.ShipDate.Read() }

        member __.ReadIf(column: Column) =
            if column.IsNull() then
                None
            else
                Some(__.Read())

    [<CLIMutable>]
    type vProductAndDescription =
        { ProductID: int
          Name: string
          ProductModel: string
          Culture: string
          Description: string }

    type vProductAndDescriptionReader(reader: Microsoft.Data.SqlClient.SqlDataReader) =
        member val ProductID = RequiredColumn(reader, reader.GetInt32, "ProductID")
        member val Name = RequiredColumn(reader, reader.GetString, "Name")
        member val ProductModel = RequiredColumn(reader, reader.GetString, "ProductModel")
        member val Culture = RequiredColumn(reader, reader.GetString, "Culture")
        member val Description = RequiredColumn(reader, reader.GetString, "Description")
        member __.Read() =
            { ProductID = __.ProductID.Read()
              Name = __.Name.Read()
              ProductModel = __.ProductModel.Read()
              Culture = __.Culture.Read()
              Description = __.Description.Read() }

        member __.ReadIf(column: Column) =
            if column.IsNull() then
                None
            else
                Some(__.Read())

    [<CLIMutable>]
    type vProductModelCatalogDescription =
        { ProductModelID: int
          Name: string
          rowguid: System.Guid
          ModifiedDate: System.DateTime
          Summary: Option<string>
          Manufacturer: Option<string>
          Copyright: Option<string>
          ProductURL: Option<string>
          WarrantyPeriod: Option<string>
          WarrantyDescription: Option<string>
          NoOfYears: Option<string>
          MaintenanceDescription: Option<string>
          Wheel: Option<string>
          Saddle: Option<string>
          Pedal: Option<string>
          BikeFrame: Option<string>
          Crankset: Option<string>
          PictureAngle: Option<string>
          PictureSize: Option<string>
          ProductPhotoID: Option<string>
          Material: Option<string>
          Color: Option<string>
          ProductLine: Option<string>
          Style: Option<string>
          RiderExperience: Option<string> }

    type vProductModelCatalogDescriptionReader(reader: Microsoft.Data.SqlClient.SqlDataReader) =
        member val ProductModelID = RequiredColumn(reader, reader.GetInt32, "ProductModelID")
        member val Name = RequiredColumn(reader, reader.GetString, "Name")
        member val rowguid = RequiredColumn(reader, reader.GetGuid, "rowguid")
        member val ModifiedDate = RequiredColumn(reader, reader.GetDateTime, "ModifiedDate")
        member val Summary = OptionalColumn(reader, reader.GetString, "Summary")
        member val Manufacturer = OptionalColumn(reader, reader.GetString, "Manufacturer")
        member val Copyright = OptionalColumn(reader, reader.GetString, "Copyright")
        member val ProductURL = OptionalColumn(reader, reader.GetString, "ProductURL")
        member val WarrantyPeriod = OptionalColumn(reader, reader.GetString, "WarrantyPeriod")
        member val WarrantyDescription = OptionalColumn(reader, reader.GetString, "WarrantyDescription")
        member val NoOfYears = OptionalColumn(reader, reader.GetString, "NoOfYears")
        member val MaintenanceDescription = OptionalColumn(reader, reader.GetString, "MaintenanceDescription")
        member val Wheel = OptionalColumn(reader, reader.GetString, "Wheel")
        member val Saddle = OptionalColumn(reader, reader.GetString, "Saddle")
        member val Pedal = OptionalColumn(reader, reader.GetString, "Pedal")
        member val BikeFrame = OptionalColumn(reader, reader.GetString, "BikeFrame")
        member val Crankset = OptionalColumn(reader, reader.GetString, "Crankset")
        member val PictureAngle = OptionalColumn(reader, reader.GetString, "PictureAngle")
        member val PictureSize = OptionalColumn(reader, reader.GetString, "PictureSize")
        member val ProductPhotoID = OptionalColumn(reader, reader.GetString, "ProductPhotoID")
        member val Material = OptionalColumn(reader, reader.GetString, "Material")
        member val Color = OptionalColumn(reader, reader.GetString, "Color")
        member val ProductLine = OptionalColumn(reader, reader.GetString, "ProductLine")
        member val Style = OptionalColumn(reader, reader.GetString, "Style")
        member val RiderExperience = OptionalColumn(reader, reader.GetString, "RiderExperience")
        member __.Read() =
            { ProductModelID = __.ProductModelID.Read()
              Name = __.Name.Read()
              rowguid = __.rowguid.Read()
              ModifiedDate = __.ModifiedDate.Read()
              Summary = __.Summary.Read()
              Manufacturer = __.Manufacturer.Read()
              Copyright = __.Copyright.Read()
              ProductURL = __.ProductURL.Read()
              WarrantyPeriod = __.WarrantyPeriod.Read()
              WarrantyDescription = __.WarrantyDescription.Read()
              NoOfYears = __.NoOfYears.Read()
              MaintenanceDescription = __.MaintenanceDescription.Read()
              Wheel = __.Wheel.Read()
              Saddle = __.Saddle.Read()
              Pedal = __.Pedal.Read()
              BikeFrame = __.BikeFrame.Read()
              Crankset = __.Crankset.Read()
              PictureAngle = __.PictureAngle.Read()
              PictureSize = __.PictureSize.Read()
              ProductPhotoID = __.ProductPhotoID.Read()
              Material = __.Material.Read()
              Color = __.Color.Read()
              ProductLine = __.ProductLine.Read()
              Style = __.Style.Read()
              RiderExperience = __.RiderExperience.Read() }

        member __.ReadIf(column: Column) =
            if column.IsNull() then
                None
            else
                Some(__.Read())

    [<CLIMutable>]
    type vGetAllCategories =
        { ParentProductCategoryName: string
          ProductCategoryName: Option<string>
          ProductCategoryID: Option<int> }

    type vGetAllCategoriesReader(reader: Microsoft.Data.SqlClient.SqlDataReader) =
        member val ParentProductCategoryName = RequiredColumn(reader, reader.GetString, "ParentProductCategoryName")
        member val ProductCategoryName = OptionalColumn(reader, reader.GetString, "ProductCategoryName")
        member val ProductCategoryID = OptionalColumn(reader, reader.GetInt32, "ProductCategoryID")
        member __.Read() =
            { ParentProductCategoryName = __.ParentProductCategoryName.Read()
              ProductCategoryName = __.ProductCategoryName.Read()
              ProductCategoryID = __.ProductCategoryID.Read() }

        member __.ReadIf(column: Column) =
            if column.IsNull() then
                None
            else
                Some(__.Read())
